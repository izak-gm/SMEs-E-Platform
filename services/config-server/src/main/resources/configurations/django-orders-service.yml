django:
  database:
    name: ${DB_NAME_ORDER}
    host: ${DB_HOST}
    port: ${DB_PORT}
    user: ${DB_USER}
    password: ${DB_PASSWORD}
  kafka:
    # TODO:signals connection
    KAFKA_BOOTSTRAP: ${KAFKA_IP4_ADDRESS}

  #  secret_key
  SECRET_KEY: ${ORDER_SERVICE_SECRET_KEY}

  server:
    host: ${DEFAULT_HOST}
    port: ${ORDERS_PORT}
