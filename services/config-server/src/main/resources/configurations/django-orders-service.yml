django:
  database:
    #    name: ${DJANGO_DB}
    name: orders
    host: localhost
    port: 5432
    #    user: ${POSTGRES_USER}
    user: postgres
    #    password: ${POSTGRES_PASSWORD}
    password: izak7901
  kafka:
    KAFKA_BOOTSTRAP: localhost:9092
    port: 9092
#  secret_key
